// pages/api/hello.js

export default function handler(req, res) {
  if (req.method === 'GET') {
    res.status(200).json({ message: 'Olá do backend integrado 🚀' });
  } else {
    res.status(405).json({ error: 'Método não permitido' });
  }
}
